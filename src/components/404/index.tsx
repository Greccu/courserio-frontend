import React, { useState, useEffect, useContext } from "react";
import { NotFoundContainer } from "./404Elements";

const NotFoundPage = () => {
  return (
      <>
        <NotFoundContainer>
          PAGE NOT FOUND
        </NotFoundContainer>
      </>
  );
};

export default NotFoundPage;